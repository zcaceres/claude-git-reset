{
  "name": "block-destructive-git",
  "version": "1.0.0",
  "description": "Claude Code hook to block destructive git commands (reset --hard, force push, etc.)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile dist/block-destructive-git",
    "build:linux": "bun build src/index.ts --compile --target=bun-linux-x64 --outfile dist/block-destructive-git-linux",
    "build:macos": "bun build src/index.ts --compile --target=bun-darwin-arm64 --outfile dist/block-destructive-git-macos",
    "build:windows": "bun build src/index.ts --compile --target=bun-windows-x64 --outfile dist/block-destructive-git-windows.exe",
    "build:all": "bun run build:linux && bun run build:macos && bun run build:windows"
  },
  "keywords": ["claude", "hook", "safety", "git"],
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  }
}
